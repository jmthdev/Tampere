{
  "name": "kotimme-tampere",
  "version": "1.0.0",
  "description": "Kotimme Tampere - Tampereen palvelut yhdessä paikassa",
  "scripts": {
    "dev": "netlify dev",
    "serve": "npx serve public -p 3000",
    "deploy": "netlify deploy --prod",
    "build": "mkdir -p public && cp node_modules/socket.io-client/dist/socket.io.js public/"
  },
  "dependencies": {
    "mongodb": "^5.9.2",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@netlify/functions": "^2.0.0",
    "netlify-cli": "^23.1.4",
    "serve": "^10.0.2"
  }
}
